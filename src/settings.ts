interface ISettings {
  title: string
  vconsole: boolean
  needPageTrans: boolean
}

const settings: ISettings = {
  title: 'H5Vue',
  vconsole: false,
  needPageTrans: true
}

export default settings